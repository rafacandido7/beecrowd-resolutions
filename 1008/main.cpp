#include <bits/stdc++.h>

using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  int n, h;
  double p;

  cin >> n >> h >> p;

  printf("NUMBER = %d\nSALARY = U$ %.2lf\n", n, h*p);

  return 0;
}
